apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: rag-platform
spec:
  selector:
    app: keycloak
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 9000
      targetPort: 9000
      name: metrics
  type: ClusterIP